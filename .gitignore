node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
.vercel
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local-only design references (heavy source files, not needed to build/deploy)
design-reference/

# Editor / local tooling
.claude/
